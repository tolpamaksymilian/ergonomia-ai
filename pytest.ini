[pytest]
# Several independent worker test packages intentionally reuse module names
# such as test_processor.py. Import isolation prevents those valid suites from
# colliding during a single repository-wide pytest run.
addopts = --import-mode=importlib
